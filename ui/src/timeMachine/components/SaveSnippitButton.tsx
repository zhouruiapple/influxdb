import React, {FC} from 'react'

// Components
import {Button, IconFont} from '@influxdata/clockface'

const SaveSnippitButton: FC = () => <Button
titleText="Save code as snippit"
text="Save Snippit"
icon={IconFont.Export}
onClick={() => {}}
/>

export default SaveSnippitButton